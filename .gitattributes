# 禁止任何行尾转换，按字节原样存取。三条理由：
#
# 1. 归档保真。批0 的 9 份全量快照与 SHA256SUMS.txt 已在 7580e60 脱敏时
#    移出工作区，但仍在 git 历史里（`git show b4629a8:archive/snapshots-2026-08-24/…`）。
#    取回时若 git 转换过行尾，哈希将与 SHA256SUMS.txt 失配。
# 2. tools/HADE-卸载器.cmd 必须是 CRLF。批处理文件被转成 LF 后
#    cmd.exe 解析会错乱 —— 实测过：goto 跳错行、命令从中间被截断。
# 3. 跨机器 clone 时文件内容不随平台变化，diff 才有意义。
* -text
