# 禁止任何行尾转换，按字节原样存取。
#
# 原因（2026-08 的旧注释说的是 archive/SHA256SUMS.txt，那批快照已在脱敏时
# 随 9 份归档一起删除，理由不再成立，换成当前真实的两条）：
#
# 1. tools/HADE-卸载器.cmd 必须是 CRLF。批处理文件若被转成 LF，
#    cmd.exe 解析会错乱 —— 实测过：goto 跳错行、命令被截断。
# 2. 仓库跨机器 clone 时不希望文件内容随平台变化，diff 才有意义。
* -text
