> ⛔ **已执行完毕的历史文件，请勿照做。**
> 其中的三处本体改动已于 2026-08-24 在原作者机器上应用完成。
> 安装请读 [`AGENT-SETUP.md`](AGENT-SETUP.md)。

# GLOBAL_PATCH — 待在本机应用的本体改动

> **本会话（Cowork 云端）连接的文件夹只有 `HADE_Vault`，够不到 `~/.claude/`。**
> 所以本体一个字未动。以下是批0 必须的两处范围扩容 + 批1 完成后的一处退役，
> 需要在**本机 Claude Code session** 里由 HADE 自己应用。

---

## ⚠ 应用前必读

1. 这三处改动**属于 §七 破坏性预警协议的适用范围**（编辑 `~/.claude/CLAUDE.md`）。
   改动 1 和 2 是**纯新增**（不删行），改动 3 涉及删除 20 行 —— 未超 30 行阈值，
   但因对象是本体，仍建议走一次预警确认。
2. 应用顺序：**1 → 2 先做**（先扩保险箱），**3 等批1 eval 通过后再做**。
3. 每处应用完毕后按 §七「HADE Vault 备份」触发条件跑一次备份。

---

## 改动 1 · §七「HADE Vault 备份」范围扩容

**位置**：§七 → `### HADE Vault 备份（本地归档）` → **动作** 列表

**性质**：新增两个备份对象。**不改任何已有条文。**

**在原动作第 3 步之后插入：**

```
3.5 `cp -r ~/.claude/skills/` → `HADE_Vault/HADE_Skills_YYYY-MM-DD/`
    （能力层已 skill 化后，skills 目录同属 HADE 身体，必须一并归档）
3.6 若 HADE-vault 仓库存在，在其中 `git add -A && git commit`
    （仓库本身即版本化备份，commit 就是回滚点）
```

**同时把原「触发」一行改为：**

```
**触发**：HADE 编辑完 `~/.claude/CLAUDE.md`（Global）、`~/.claude/references/` 下任何文件、
或 `~/.claude/skills/` 下任何 SKILL.md，且内容实际发生改变后。
```

**理由**：拆分开始的那一刻起，HADE 的身体就有一部分在 `~/.claude/skills/` 下。
备份范围不跟着走 = **身体的一部分不在保险箱里**。这是批0 不可跳过的原因。

---

## 改动 2 · §七「破坏性操作预警协议」适用范围扩容

**位置**：§七 → `### HADE 体系破坏性操作预警协议` → **适用范围（仅限 HADE 本体，不含项目）**

**性质**：新增一行。**不改任何已有条文。**

**在 `~/.claude/references/*.md`（参考库）之后插入：**

```
- `~/.claude/skills/*/SKILL.md`（能力层）
- `HADE_Vault/HADE-vault/`（plugin 仓库）
```

**理由**：同上。预警协议保护的是「HADE 本体」这个集合，集合的成员随形态变化，
保护范围必须同步。否则 skill 化之后，能力层处于**无预警裸奔**状态。

---

## 改动 3 · §十一「HADE 参考库」整章退役（**等批1 eval 通过后再做**）

**位置**：§十一 全章（1071–1090 行，共 20 行）

**性质**：**删除整章**。这是本轮唯一的删除操作。

**前置条件（缺一不可）：**
- [ ] 3 个 skill 已装入 `~/.claude/skills/`（或经 plugin 安装）
- [ ] `run_eval.py` 实测：3 条正例触发率达标，1 条负例不误触发
- [ ] 至少一次真实 session 里，skill 在该触发的场景确实被唤起

**为什么可以整章删**：§十一 的全部内容是一张**手工维护的检索索引表**
（"什么时候读哪个 references 文件"）。skill 机制**自动**做这件事 ——
description 就是索引，harness 就是检索器。索引表被机制取代，
留着 = 两套索引 = §七「不另开口子」。

**同时**：`~/.claude/references/` 下的 3 个 .md 文件在确认 skill 生效后删除，
避免同一内容两处存在（§七 单一源）。**删除前确认 Vault 归档里有副本**
（本仓库 `archive/snapshots-2026-08-24/` 已存，SHA256 见 `archive/SHA256SUMS.txt`）。

---

## 边界说明（为什么这三处不算违反「只动形式不动内容」）

Plan §1 非目标写的是「不改任何**规则的内容**」。

- 改动 1、2 改的是**范围声明**——"保护哪些文件"。规则本身（怎么备份、怎么预警）一字未动。
  形态变了，范围跟着走，这是**同一条规则的正确执行**，不是改规则。
- 改动 3 删的是**索引表**，不是规则。表里没有任何行为约束，只有"文件在哪"。

如果这三处不做，就会出现：**新形态下旧规则指向不存在的范围** —— 那才是真正的内容漂移。
