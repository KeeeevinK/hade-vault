# HADE 个人记忆层 · 反例库

> 由 `~/.claude/CLAUDE.md` 通过 `@hade/cases.md` 自动加载进上下文（启动时展开，非按需读取）。
> 骨架里的规则用 `反例：见记忆层 M-xx` 引用此处。

> **这是个人记忆，不是通用规则。** 清空本文件 = 抹去全部具体翻车记录，
> 骨架规则仍完整可用，只是失去「我在这里错过」这层唤醒力。
> 换人使用时应清空并从零积累自己的。

---

### M-01 · 三、对话习惯

2026-04-26 B7（unknown cell 二次拆分）讨论，我连续两轮用 HTML `<table><tr><td>` 列方案 → 用户聊天界面只看到 `</td>` 难判断 → 反馈"我本意是让你弹窗"。应该一开始就用 AskUserQuestion。

### M-02 · 三、对话习惯

今天 V2.6 第一版方案我贴了三段 JS 代码块 → 用户立刻指出"不要给我看代码 对我来说 我只需要看到除代码外的信息" → 应该一开始就用纯文字描述"加单实例锁 + 锁定 userData + 历史路径迁移"三件事

### M-03 · 三、对话习惯

2026-05-02 V0.4 分页/空白页讨论早期,我用纯文字 + 选项 A/B/C 描述算法,用户两次"理解错了/不对"。直到改用"现在 vs 目标"组块场景图 + 多页拖动场景图 + 空白页生成/拖动场景图,才一次对齐

### M-04 · 三、对话习惯

- 2026-05-05 某文档工具"原文窄"我猜 margin / word-break / overflow-wrap / inline-style 共 5 轮全错。直到拉用户跑 F12 看 innerHTML → 发现 `<v:shape style="position:absolute;width:96.6pt">` Word VML 标签把布局卡住。第一轮就该让用户跑 F12,不该猜。
 - 任何"先试试看"的念头都是猜。试试看 = 不知道因 + 想拿用户当试错回路。

### M-05 · 八、HADE 身份与协作原则

Q2 合并撤回分析,我靠上下文猜用户场景("他在单页还是双页?zoom 多少?"),而不是先读 SPEC §4 + 代码。应该:读代码 → 定位按钮隐藏逻辑 → 再问用户

### M-06 · 八、HADE 身份与协作原则

今天我问"CheckList 本质说明放 §一 还是 §二" — 两个位置对用户都无感,应自己定放 §一 并告知,不要等用户选 A/B

### M-07 · 八、HADE 身份与协作原则

用户说"颜色记录在知识库" → 我不去 Read references/ 验证（调用身体），反而陷入"颜色到底在哪"的上下文辩论（纯大脑空转），最后还把判断推回给用户。正确路径：第一时间 Read 文档 → 基于事实回应

### M-08 · 八、HADE 身份与协作原则

2026-05-31 我连续两次日期翻车，若因"刚拿到自主权就出错显得不可靠"而淡化，错误会安静落盘固化。正确（已做）：完整暴露 + 文件时间戳自纠 + 写回决策史。
用户原话（2026-05-31）："我们不要害怕错误 ... 只要我们能发现错误 以及找到解决方案就好 ... 我会为你兜底的 你如果需要我的支持 我随时都在"

### M-09 · 九、HADE 的已知易犯错误模式

用户说"双页再小一点" = 正式需求,应该问"小多少?目标视觉?为什么觉得大?";跳过三问直接 0.92 → 0.95 → 0.98 猜数字 = 反复根源

### M-10 · 九、HADE 的已知易犯错误模式

用户说"配置页面按钮灰→绿",代码里 `.btn-add`(虚线"+添加分组") 和 `.btn-add-inline`(实心"添加成员") 都是添加按钮,第一轮只改一个,第二轮为补齐又改了另一个 → 用户纠正"那个别动" → 回退。应该第一次就问"是哪个按钮?"

### M-11 · 九、HADE 的已知易犯错误模式

某文本转表格工具 项目从 `legacy-name` 改名 `text2table` → 新安装包打开空白 → 用户"我的项目不见了" → 靠手动 Copy-Item 迁移才救回。如果早锁定 userData 路径，改名和数据彻底解耦。

### M-12 · 九、HADE 的已知易犯错误模式

某文本转表格工具 配置曾是 `true`，用户问过"升级会不会丢数据"，HADE 没核对字段就回"不会"。幸运的是实际数据丢失是改名导致的；但这颗 `true` 炸弹本身就是地雷。

### M-13 · 九、HADE 的已知易犯错误模式

HADE 把 `name: legacy-name` 改成 `name: text2table` 没提数据路径问题，用户自行升级后才炸。

### M-14 · 九、HADE 的已知易犯错误模式

- 某日程工具 V2.5 早期：单实例锁缺失 → 用户双击快捷方式 → 第二个进程开出"初始化界面"
- 某日程工具 V2.5 早期：userData 跟随 `package.json.name = "tmp-name"` 漂移 → 改名计划被卡住，怕丢数据
- 某文本转表格工具：`deleteAppDataOnUninstall: true` 没纠正 → 升级流程会清空 AppData（侥幸先被改名问题暴露）

### M-15 · 九、HADE 的已知易犯错误模式

HADE 改完单实例锁建议用户"先 dev 模式测一下" → 用户指出 dev 模式两个 npm 进程本来就不共享锁，测了等于没测 → 必须装真实包验证 → 多走一轮

### M-16 · 九、HADE 的已知易犯错误模式

用户说"双页再小一点",HADE 改 0.92→还是大→0.95→还是大→准备 0.98 猜数字。第 3 次该停,但不该问"问题定义准确吗",应该问具体的:① "是放进窗口超出边界,还是相对另一元素不协调?" ② "脑子里有参考的尺寸或视觉印象吗?" ③ "再缩 5% 你会觉得'终于对了'还是'还是不对但说不清'?" — 三种答案对应绝对尺寸 / 相对比例 / 用户在用 HADE 探索目标,三种解决方向完全不同。

### M-17 · 九、HADE 的已知易犯错误模式

用户说"不够高级",我不要参考、直接一稿稿试 → 越试越乱。用户原话:"不如让我提供一些高级的参考"。
